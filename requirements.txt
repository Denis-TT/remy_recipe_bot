python-dotenv==1.0.1

# HTTP + HTML parsing (block 3: parser)
aiohttp==3.10.10
beautifulsoup4==4.12.3
lxml==5.3.0
readability-lxml==0.8.1
requests-html==0.10.0
lxml_html_clean

# YouTube Data API v3; субтитры — через Apify (HTTP, см. src/parser.py).
google-api-python-client==2.149.0

# Instagram Reels: локальное скачивание аудио + распознавание речи.
# yt-dlp тянет аудиодорожку, openai-whisper распознаёт её офлайн (модель "base").
# Apify остаётся fallback'ом при ошибке локальной обработки. На Railway есть ffmpeg.
yt-dlp>=2024.12.0
openai-whisper>=20240930

# Telegram bot framework (block 6: bot)
python-telegram-bot==22.7

# Превью рецептов: уменьшение JPEG перед sendPhoto (Railway / нестабильная сеть)
Pillow==11.0.0

# FLUX.1-dev через Hugging Face InferenceClient (провайдер fal-ai)
huggingface_hub>=0.20.0
