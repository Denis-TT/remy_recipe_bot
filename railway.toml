[build]
builder = "NIXPACKS"

[build.config]
pythonVersion = "3.11"

[deploy]
startCommand = "pkill -9 -f 'python.*run.py' 2>/dev/null; sleep 2; python run.py"
numReplicas = 1
restartPolicyType = "ON_FAILURE"
